{
  "name": "@tirrenia/palette",
  "private": false,
  "version": "1.1.0",
  "type": "module",
  "files": [
    "dist/colors.css",
    "dist/logos/"
  ],
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run build:assets",
    "build:assets": "mkdir -p dist/logos && cp src/colors.css dist/colors.css && cp assets/logo-*.png assets/logo-*.svg dist/logos/ 2>/dev/null || true",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}

